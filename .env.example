# 이 파일을 .env 로 복사해서 실제 값을 넣으세요. .env 는 git 에 커밋되지 않습니다.

# LLM (Anthropic Claude)
ANTHROPIC_API_KEY=sk-ant-...
ANTHROPIC_MODEL=claude-haiku-4-5-20251001

# csrng
CSRNG_BASE_URL=https://csrng.net

# 외부 API 장기 장애 시 bypass 허용 여부 (기본 false, true 면 csrng 호출 실패를 성공으로 간주)
CSRNG_BYPASS_ON_OUTAGE=false
